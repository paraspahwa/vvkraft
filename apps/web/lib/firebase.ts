// Firebase client SDK has been replaced by Better Auth.
// This file is kept as a stub to prevent import errors during migration.
// All authentication is now handled by Better Auth (see lib/auth.ts and lib/auth-client.ts).
// All database operations use Supabase (see lib/supabase.ts and lib/db.ts).

export const app = undefined;
export const auth = undefined;
export const db = undefined;
export const storage = undefined;
